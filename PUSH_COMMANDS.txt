After creating your GitHub repository, run these commands:

git remote add origin https://github.com/YOUR_USERNAME/geo-attendance-pro.git
git branch -M main
git push -u origin main

Replace YOUR_USERNAME with your actual GitHub username.

Example:
If your GitHub username is "johnsmith", the command would be:
git remote add origin https://github.com/johnsmith/geo-attendance-pro.git